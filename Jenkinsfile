pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                echo 'Repo linked successfully'
            }
        }

        stage('Build') {
            steps {
                echo 'Build stage running'
            }
        }

        stage('Test') {
            steps {
                echo 'Test stage running'
            }
        }
    }
}
